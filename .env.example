# Copy this file to .env and fill in your key to enable the real draft() seam
# (backend/pipeline/draft.py). Without this, the pipeline falls back to the
# offline hardcoded sample — no key needed to run demo.py or the tests.
GROQ_API_KEY=