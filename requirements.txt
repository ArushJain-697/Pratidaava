# Scaffold runtime. The pipeline itself needs only pydantic; fastapi/uvicorn are for the API+UI.
pydantic>=2.0
fastapi>=0.110
uvicorn>=0.29
# Optional: only imported if present. draft.py falls back to the offline sample when
# this isn't installed or AWS credentials/model access aren't configured — install it
# to use the real Bedrock seam (ticket #8).
boto3>=1.34